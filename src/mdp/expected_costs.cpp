#include "mdp.ih"

/*
 *  Calculate the expected cost for a given state and action
 */

num_t MDP::expected_costs(size_t state1, size_t state2, num_t prodRate1, num_t prodRate2)
{
    num_t tmpVal = 0;
    num_t bestVal = 0;


}